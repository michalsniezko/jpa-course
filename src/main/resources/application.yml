spring:
  application:
    name: jpa-course
  datasource:
    url: jdbc:postgresql://localhost:5432/data_jpa
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
